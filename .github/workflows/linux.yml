name: linux

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  ubuntu-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    # Error Check
    - name: Run init.sh
      run: |
        bash ${GITHUB_WORKSPACE}/bin/init.sh 2> error.log; cat error.log
        if [ ! -s error.log ]; then true; else false; fi

    - name: Load .bash_profile
      run: |
        bash ~/.bash_profile 2> error.log; cat error.log
        if [ ! -s error.log ]; then true; else false; fi

    - name: Load .bashrc
      run: |
        bash ~/.bashrc 2> error.log; cat error.log
        if [ ! -s error.log ]; then true; else false; fi
